# Local data (download / ingest / export; not for GitHub)
/data/raw/
/data/db/
/data/cache/
/data/*.csv

# Python
.venv/
__pycache__/
*.py[cod]
.Python

# Secrets
.env
.env.local

# Frontend
frontend/node_modules/
frontend/dist/
frontend/coverage/

# OS
Thumbs.db
.DS_Store

# Throwaway deploy/diag scripts (prefix `_`); keep bootstrap_services.sh
deploy/_*.sh
deploy/_*.ps1

# scripts/ — anything prefixed with `_` is throwaway diag / logs (keep the 6
# 'real' scripts: check-venv.js, clean-ports.ps1, count_empty_tickers.py,
# dev-api.js, nightly_ingest.sh, smoke_apis.py)
scripts/_*
scripts/__pycache__/
scripts/cloudflared/
scripts/deploy/
*.err.log
.dev-*.log
node_modules/
